<table>
  <tbody>
      <% post.post_comments.each do |post_comment| %>
      <tr>
        <td>
          <%= link_to user_path(post_comment.user) do %>
            <%= image_tag post_comment.user.get_profile_image, size: "80x80" %><br><br>
            <%= post_comment.user.name %>
          <% end %>
        </td>
        <td><%= post_comment.comment %></td>
        <td>
            <%= link_to "Destroy", admin_post_path(post, post_comment), method: :delete, remote: true, class: "btn btn-danger pull-right" %>
        </td>
      </tr>
      <% end %>
  </tbody>
</table>