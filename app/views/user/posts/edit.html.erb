<main>
  <div class= "container">
    <div class= "row">
      <div class= "col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        </br>
          <h1>投稿編集画面</h1>
          <% if @post.errors.any? %>
            <h3><%= @post.errors.count %> error prohibited this obj from being saved:</h3>
            <ul>
              <% @post.errors.full_messages.each do |message| %>
              <li><%= message %></li>
              <% end %>
            </ul>
          <% end %>
          <%= form_with model: @post, url: post_path(@post.id), method: :patch do |f| %>
            <%= f.label :タイトル %></br>
            <%= f.text_field :title, autofocus: true, id:"inputtitle", placeholder:""%></br></br>
            <%= f.label :本文 %></br>
            <%= f.text_area :body, autofocus: true, id:"inputboby", placeholder:""%></br></br>
            <%= f.submit "更新", class: "btn btn-success" %>
          <% end %>
          </br>
          <%= link_to "投稿詳細", post_path(@post.id) %>
          |
          <%= link_to "一覧に戻る", posts_path %>
      </div>
    </div>
  </div>
</main>